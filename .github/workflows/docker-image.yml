name: Docker Image CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: docker login
      env:
        Docker_USER: ${{secrets.Docker_USER}}
        Docker_PASSWORD: ${{secrets.Docker_PASSWORD}}
      run: docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
    - name: build the docker image
      run: docker build . --file Dockerfile --tag elyesntc/testtodockerhub:v1
    - name: docker push
      run: docker push ${{secrets.DOCKER_USER}}/testtodockerhub:v1
